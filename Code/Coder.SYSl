require monitor;

lang-imp SYSlang_testV;

from SYSlang_testV enter coder;

for Coder{
    dl(from(systemware.ga/coder/ enter code).to('A:/'));
    return;
} else{
    GUI.force(exit);
    exe nogui;
    return;
}